# 3Dling Role Windows

Project path: `D:\javaProjects\personal\3Dling`
Updated: 2026-06-20

## Registry

| Role | Status | Thread ID | Scope | Owns | Must Not Own | Next Action |
| --- | --- | --- | --- | --- | --- | --- |
| 架构 | 接续中 | `019ecf57-0544-7120-ae3b-d2b0ecb0df30` | 需求澄清、技术方案、方案拆分、角色窗口台账 | 需求边界、复杂新需求的多方案技术方案、场景结构、下游任务拆分、验收口径 | 直接实现、生成最终 3D 模型、提交代码 | 新复杂需求先产出多方案技术方案并确认路线，再派发 UI/Frontend、开发、QA |
| UI/Frontend | `sence2/3/4/6` 舞台内侧落点与 `sence1-6` 脚底阴影已回调并通过架构验收 | `019ee0eb-b1e4-7a33-ac59-d7af30e88c7c` | 纯前端视觉体验与场景构图 | 视觉方向、参考图对齐、背景/玩偶/照片栈构图、响应式视觉验收、截图/crop 标准；已完成 `sence2/3/4/6` 玩偶从舞台边缘修到舞台顶面内，并完成 `sence1-6` 玩偶脚底与 contact shadow 贴合检查 | 生成最终 3D 模型、修改玩偶 PNG/原始照片、纳入 sence7/8、改告白页/照片墙内容、破坏 `sence1` / `sence5` 已验收基线、扩大成全场景重排 | 本轮架构已复核截图、运行资产校验/构建/全视口场景 QA；后续如继续视觉精修，仍优先派发 UI/Frontend 并要求主动回调架构 |
| 开发 | `sence5` 首屏视觉链路已验收；后续作为实现窗口 | `019ecfa1-bd0b-74b0-9e37-53e279a857e5` | 前端静态礼物页代码实现 | Vite/React/Three.js 页面、数据驱动 manifest、单 Three.js 舞台、移动端与桌面端验证、资产闸门和 QA 脚本 | 删除原始照片、生成 3D 玩偶资产、纳入 sence7/8、在线上传/在线生成、在无 UI/Frontend 视觉方案时主导视觉方向 | 等待架构或 UI/Frontend 派发下一步；保持 `sence5` 生成式背景、照片栈、光柱/玩偶响应式锚点和平台落点基线稳定 |
| QA | 已建立，待独立验收触发 | `019ed3c9-6759-7252-aeff-cdcf6619a375` | 首版可展示前的只读验收与回归检查 | 评审/发布就绪度、阻塞项验证、桌面/移动端关键流程、控制台/网络异常、验收风险记录 | 实现功能、修代码、生成正式测试用例/测试报告、扩大范围到 sence7/8 或在线生成 | 如需发布前第三方把关，发送 `sence5` 首版只读验收任务，并要求完成后主动回调架构窗口 `019ecf57-0544-7120-ae3b-d2b0ecb0df30` |
| 3D 生成 skill | 长期备选，暂停阻塞首版 | `019ecfa2-a2fa-7ed1-a2c4-b9062e6aed03` | 照片到 2D 概念图到 3D 玩偶资产流程 | 创建 `photo-to-cute-3d-toy` skill、定义 manifest/提示词/输出路径、优先跑通 sence5 | 实现前端、删除照片、擅自使用名人/IP 标识、擅自下载超大模型或长时间重装环境 | 暂停默认推进；后续可做 `sence5-v2.glb`，但首版展示先走前端 2.5D 玩偶 |

## Accepted Architecture Notes

- First release includes `sence1` through `sence6`, then a confession summary screen and a photo-wall section for future updates.
- This is a pure frontend visual project. The earlier flow overused the `开发` window as a catch-all because it was the established implementation window. Future visual requirements should instead use `UI/Frontend` as the visual owner, with `开发` only implementing accepted specs.
- For any new complex 3Dling requirement, `架构` must first write a multi-option technical plan covering visual route, asset route, frontend implementation route, transition route, and QA route. Do not dispatch implementation until the selected route is clear.
- Project-specific role and visual lessons are recorded in `docs\frontend-role-and-visual-lessons.md`.
- `sence7` and `sence8` are explicitly out of scope for the first release.
- `sence1`、`sence2`、`sence3`、`sence4`、`sence6` 的首版扩展方案已沉淀到 `docs\scene-expansion-plan.md`。方案建议沿用 `sence5` 已验收路线：生成式全屏背景图、2.5D 透明玩偶、照片叠放、单 Three.js 舞台、背景 crossfade、必要时用 `toyDisplay.responsivePositions` 做响应式锚点校准。等待用户确认后再派发资产生成和开发实现。
- User confirmation on 2026-06-19: `sence2`、`sence3`、`sence4`、`sence6` 的方向已接受；`sence1` 需要从普通童年房间调整为“大理白族传统民居 / 白族院落里的童年记忆小舞台”，背景核心应体现白墙青瓦、照壁、木雕窗花、庭院或石板地。
- The web page is static: photos and toy/model assets are prepared ahead of time; no online upload or live generation in the page.
- Each scene shows one main photo and one current toy. Multiple photos can be used as generation references, but the UI should only show one main photo per scene.
- All scenes should share one Three.js stage/canvas. Scene changes should swap/fade the current toy so only one toy is visible at any time.
- `sence5` visual baseline is approved: left-side beach/travel main photo, right-side red wheat-field concert stage, one cute toy using face from `photos\sence5\1.jpg` and outfit/body references from `photos\sence5\2.jpg` or `photos\sence5\3.jpg`.
- Avoid direct celebrity/brand/IP use. For the Haikou concert memory, use abstract elements such as red haze, wheat field, stadium darkness, ring lights, spotlight, ticket stub, and travel props.
- Role discipline reminder from 2026-06-17: the architecture window should not directly edit frontend implementation for visual polish or bug fixes. For future UI changes, architecture defines the acceptance target and dispatches the task to the development window, then verifies the callback. Direct architecture edits should be limited to role registry / handoff documentation unless the user explicitly asks this window to implement.
- Background direction decision on 2026-06-17: for `sence5`, use a high-quality generated full-screen background image as the main visual carrier for the red wheat concert scene, because matching the preview's photo-grade wheat, smoke, light, and stadium depth is cheaper and better with a static art plate than with low-poly Three.js geometry. Frontend should provide a reusable scene-background asset layer with preloading and crossfade/opacity transitions so scene changes do not hard-cut. Three.js should focus on the toy, stage/platform, and subtle dynamic accents.
- Architecture acceptance on 2026-06-20 after UI/Frontend Option B polish: passed. UI/Frontend thread `019ee0eb-b1e4-7a33-ac59-d7af30e88c7c` moved `sence1/2/3/4/6` toward the accepted `sence5` visual language without replacing background files in this pass. The implementation data-drivenizes `photoLayout`, `copyMask`, and `backgroundAsset.mask`; non-`sence5` photos move left with reusable stacked-card styling; left-side masks are lighter; `StageCanvas` now uses outgoing/incoming sprite fade/scale transition and reports `data-sprite-transition`. Architecture reran `npm run validate:toys`, `npm run validate:asset-gate`, `npm run build`, and `npm run qa:scenes -- --screenshots --viewport 1398x986 --viewport 1440x900 --viewport 1920x1080 --viewport 2048x1117 --viewport 390x844`; all passed, with only the existing Three.js chunk-size warning in build. Evidence screenshots: `C:\Users\12156\AppData\Local\Temp\3dling-ui-final-20260620014410\contact-sheet.png` and `C:\Users\12156\AppData\Local\Temp\3dling-scene-qa-1781891386464`.
- Architecture acceptance on 2026-06-20 after `sence1` toy platform-fit narrow fix: passed. UI/Frontend thread `019ee0eb-b1e4-7a33-ac59-d7af30e88c7c` changed only `src\data\scenes.ts` for `sence1.toyDisplay`, reducing height to `1.52` and setting desktop/mobile positions to `[-0.2, 1.1, 0.08]` / `[-0.08, 0.45, 0.08]`. The background image and background position were not changed. Architecture visually checked `1398x986` and `2048x1117` foot crops: both shoe soles now sit on the white-stone platform top plane, with the platform front edge below the feet. Architecture reran `npm run validate:toys`, `npm run validate:asset-gate`, `npm run build`, and `npm run qa:scenes -- --screenshots --viewport 1398x986 --viewport 1440x900 --viewport 1920x1080 --viewport 2048x1117 --viewport 390x844`; all passed, with only the existing Three.js chunk-size warning. Evidence screenshots: `C:\Users\12156\AppData\Local\Temp\3dling-s1-fit-final-20260620120405\1398x986-sence1-feet-crop.png`, `2048x1117-sence1-feet-crop.png`, `1398x986-sence1.png`, and `1398x986-sence5.png`.

## 3D Generation Review Notes

- `photo-to-cute-3d-toy` was created at `C:\Users\12156\.codex\skills\photo-to-cute-3d-toy`.
- Skill validation passed with bundled Python on 2026-06-16.
- `docs\3d-generation\toy-scenes.manifest.json` and `docs\3d-generation\sence5-prompt-pack.md` reproduce cleanly through `scripts\build_prompt_pack.py`.
- Recommended `sence5` concept image is `assets\toy-concepts\sence5-concept-v3.png`.
- `public\models\sence5.glb` was generated on 2026-06-17 and independently checked by the architecture window.
- Current GLB validation: 280,032 bytes; magic `glTF`; Three.js `GLTFLoader.parse` passed with `meshes=1`, `materials=1`.
- The model was generated locally with TripoSR; the 3D window reported no external upload of private photos or concept images.
- Treat the first GLB as frontend-connectable prototype quality, not final art polish.

## Frontend Review Notes

- First Vite/React/Three.js version is implemented and visually usable.
- Development validation reported `npm run build` passing, with only the non-blocking Three.js chunk-size warning.
- The active navigation bug for `#letter` and `#wall` was fixed by separating navigation section state from the Three.js stage scene state.
- Desktop and mobile QA reported: `#sence5`, `#letter`, and `#wall` active states correct; `canvasCount = 1`; no console/network issues; no horizontal overflow.
- `sence5.glb` technical integration passed on 2026-06-17: `sence5` renders with `data-model-state="glb"`, other scenes remain placeholder, `#letter`/`#wall` nav active states pass, no console/network issues, no horizontal overflow.
- Architecture visual decision on 2026-06-17: current GLB is not display-approved. It reads as a low-fidelity bas-relief/cardboard standee rather than a cute 3D toy. Treat as a technical integration sample only.
- Architecture visual check on 2026-06-17: current `sence5` 2.5D sprite loads correctly (`data-model-state="sprite"`) but is not display-approved yet. Main issues: photo sits too close to center/stage boundary, sprite material is not visibly lit, and the red wheat concert stage lacks the approved preview's spotlight, haze, foreground lamps, and depth.
- Architecture visual check on 2026-06-17 after development rework: `sence5` is much closer to the approved preview, with left-anchored photo, visible spotlight, denser red wheat, and scoped haikou stage effects. It still is not display-approved: at 1920px the photo overlaps the `海口` title, the toy still reads as standing inside foreground wheat rather than on a clear circular platform, and the haikou stage logic should be nudged toward a reusable stage preset before extending to other scenes.
- User reminder on 2026-06-17: the approved preview also depends on the left photo stack effect. `sence5` should show a main photo with 2+ offset dark/gray backing cards, and the stack treatment should be implemented as a reusable photo-stack pattern rather than a one-off decorative hack.
- Architecture final visual acceptance on 2026-06-17 after sence5 second rework: `sence5` is display-approved for the first 2.5D release. CodeGraph confirmed `stage.profile: "red-wheat-concert"` drives the reusable concert preset, while `StorySection` uses two real `.photo-shadow` layers. Local `npm run build` passed with only the existing Three.js chunk-size warning. Browser plugin could open the page but `Page.captureScreenshot` timed out, so architecture fallback used Playwright + local Chromium. Accepted screenshots: `C:\Users\12156\AppData\Local\Temp\3dling-arch-accept-nav-1781687154104\desktop-1920x1080-sence5.png`, `desktop-1440x900-sence5.png`, and `mobile-390x844-sence5.png`. Known non-blocking gap: the stage remains more stylized/Three.js-abstract than the target preview's photo-grade haze, foreground bulbs, and background screen; treat further realism as polish, not a blocker for the first scene approval.
- User observation on 2026-06-17: toys across all scenes read as too close to the bottom of the screen. Do not solve this by one-off per-scene nudges. After the current `sence5` preview-fidelity pass is accepted, run a separate shared layout pass to fix the Three.js stage camera/toy/floor anchor model so each scene has a stable, higher, reusable toy position on desktop and mobile. Validate at least `sence1`, `sence5`, `sence6`, `#letter`, and `#wall`.
- Architecture final visual acceptance on 2026-06-17 after sence5 preview-fidelity polish: `sence5` remains display-approved and is closer to the target preview. CodeGraph confirmed the new layered smoke, textured platform, lamp glow/reflections, stadium bulbs, and abstract LED screen remain scoped to the `red-wheat-concert` preset. Local `npm run build` passed with only the Three.js chunk-size warning. Browser plugin initialized and reached the page, but `Page.captureScreenshot` timed out and its active-section state did not settle, so architecture fallback used Playwright + local Chrome. Accepted screenshots: `C:\Users\12156\AppData\Local\Temp\3dling-arch-s5-polish-1781689413088\desktop-1920x1080-sence5.png`, `desktop-1440x900-sence5.png`, and `mobile-390x844-sence5.png`. Remaining required follow-up: shared stage/toy anchor pass, because `sence1`, `sence5`, and `sence6` still read too low on the screen.
- Architecture acceptance on 2026-06-17 after shared stage/toy anchor pass: passed for the first release layout baseline. CodeGraph confirmed `STAGE_ANCHORS` / `getStageAnchor(width)` centralize desktop and narrow toy anchors in `StageCanvas`, and `docs/frontend-manifest.md` now documents the rule to adjust shared anchors before per-scene corrections. Local `npm run build` passed with only the existing Three.js chunk-size warning. Browser plugin initialized and reached the page; architecture used Playwright + local Chrome for the screenshot matrix and real rail-click navigation. Verified `#sence1 -> #sence5 -> #sence6 -> #letter -> #wall`, `canvasCount = 1`, `sence5` remains `data-model-state="sprite"`, `sence1/sence6` remain placeholders, with no console/page/request errors and no horizontal overflow. Accepted screenshots: `C:\Users\12156\AppData\Local\Temp\3dling-arch-anchor-1781691392122\desktop-1440x900-sence1.png`, `desktop-1440x900-sence5.png`, `desktop-1440x900-sence6.png`, `desktop-1920x1080-sence5.png`, `mobile-390x844-sence1.png`, `mobile-390x844-sence5.png`, and `mobile-390x844-sence6.png`. Known non-blocking gap: mobile toys, especially `sence5`, still read low/dim compared with the preview; treat that as a separate mobile composition/lighting polish pass rather than more one-off anchor nudges.
- Architecture check on 2026-06-17 after generated full-screen background pass: `sence5` background direction is visually approved. Playwright + local Chrome clicked-navigation captures show a full-screen generated red wheat concert background with no photo-right hard boundary, denser wheat texture, red haze, stadium depth, spotlight, and foreground lamp reflections. Local `npm run build` passed with only the existing Three.js chunk-size warning; `public\backgrounds\sence5-red-wheat-concert.png` exists as a 1672x941 PNG. Blocking issue found before final acceptance: directly opening `http://127.0.0.1:5173/#sence5` still initializes at `#sence1` (`activeRail="#sence1"`, placeholder model, default stage profile, `scrollY=0`). Return to development for a narrow direct-hash initialization fix while preserving the accepted background/crossfade work.
- Architecture acceptance on 2026-06-17 after direct hash narrow fix: passed. `src\App.tsx` is the only implementation file changed in the callback, using `getHashSectionId()`, initial `useLayoutEffect` hash scrolling, and a `hashchange` listener to synchronize `activeSectionId` / `stageSceneId`. Local `npm run build` passed with only the existing Three.js chunk-size warning. Browser plugin could read DOM state but still had unreliable hidden-tab scroll/screenshot behavior (`Page.captureScreenshot` timeout), so final acceptance used Playwright + local Chrome. Verified direct loads for `#sence5`, `#sence6`, `#letter`, and `#wall`; `#sence5` direct load now reports `activeRail="#sence5"`, `targetTop=0`, `data-model-state="sprite"`, `data-stage-profile="red-wheat-concert"`, `canvasCount=1`, and no horizontal overflow. Also verified desktop 1440, desktop 1920, and mobile 390 direct `#sence5`, plus clicked navigation `#sence1 -> #sence5 -> #sence6` with exiting/active background crossfade and no console/page/request errors. Accepted screenshots: `C:\Users\12156\AppData\Local\Temp\3dling-arch-direct-verify-1781705915611\sence5-1398x986.png`, `sence5-desktop-1440-1440x900.png`, `sence5-desktop-1920-1920x1080.png`, `sence5-mobile-390-390x844.png`, and `nav-final-sence6-1398x986.png`.
- User visual issue on 2026-06-18: `sence5` toy still does not read as standing on the stage. Architecture reproduced with Playwright + local Chrome. Current desktop screenshot shows the 2.5D toy's boots sitting on or below the foreground lamp ring/front platform edge rather than on the circular platform top; mobile screenshot makes the toy read as floating in the wheat/text area instead of grounded on the visible platform. This is a narrow composition/grounding fix, not a request to redo the generated background, hash routing, photo stack, or all-scene anchors. Evidence screenshots: `C:\Users\12156\AppData\Local\Temp\3dling-s5-stage-fit-long-1781745116342\sence5-1398-after-6000.png`, `C:\Users\12156\AppData\Local\Temp\3dling-s5-stage-fit-1781745003130\desktop-1440-sence5.png`, and `C:\Users\12156\AppData\Local\Temp\3dling-s5-stage-fit-1781745003130\mobile-390-sence5.png`.
- Architecture acceptance on 2026-06-18 after `sence5` toy platform-fit narrow fix: passed. Code inspection confirmed the callback stayed narrow: `src\data\scenes.ts` adds optional `toyDisplay.mobilePosition` and adjusts only `sence5` sprite desktop/mobile positions; `src\components\StageCanvas.tsx` uses mobile position below 600px and tightens sprite contact shadow. Local `npm run build` passed with only the existing Three.js chunk-size warning. Browser plugin DOM check passed for direct `#sence5`; Playwright + local Chrome verified direct `#sence5` at 1398x986, 1440x900, and 390x844, plus clicked navigation `#sence1 -> #sence5 -> #sence6`, with `canvasCount=1`, no console/page/request errors, no horizontal overflow, and preserved background crossfade. Visual acceptance: desktop toy boots now sit inside the circular platform top rather than on the foreground lamp ring/front edge; mobile no longer reads as floating beside the wheat/text, though the 2.5D contact shadow remains a minor non-blocking art residual. Accepted screenshots: `C:\Users\12156\AppData\Local\Temp\3dling-arch-platform-accept-1781746761417\desktop-1398x986-sence5.png`, `desktop-1440x900-sence5.png`, and `mobile-390x844-sence5.png`.
- Correction on 2026-06-18: the previous `sence5` toy platform-fit acceptance was too loose and is withdrawn. User supplied annotated screenshot `C:\Users\12156\AppData\Local\Temp\codex-clipboard-7cda6b0b-8ed4-42de-8ce9-cb997a0b6f62.png` showing the toy boots still below/crossing the circular platform front edge; the arrow marks the intended top stage surface. New hard acceptance: both boot soles must visually sit on the circular platform top surface, with a contact shadow/reflection on that top plane, and no visible part of the feet reading as under or in front of the platform rim.
- Architecture acceptance on 2026-06-18 after hard `sence5` platform-fit fix: passed. Code inspection confirmed the callback remained scoped to `src\data\scenes.ts`, `src\components\StageCanvas.tsx`, and `docs\frontend-manifest.md`: `sence5.toyDisplay.height` is reduced to `2.04`, desktop position is `[-0.3, 1.12, 0.08]`, mobile position is `[-0.28, 0.32, 0.08]`, and the existing sprite shadow remains tightened to the shoe contact plane. Local `npm run build` passed with only the existing Three.js chunk-size warning. Browser DOM verification for direct `#sence5` passed; Playwright + local Chrome verified direct `#sence5` at 1398x986, 1440x900, and 390x844 plus clicked navigation `#sence1 -> #sence5 -> #sence6`, with `canvasCount=1`, no console/page/request errors, no horizontal overflow, and preserved background crossfade. Visual acceptance: desktop full and cropped screenshots show both boot soles clearly on the circular platform top surface, with the platform front rim below the feet; mobile still has 2.5D scale/crop compromises but reads as standing on the visible platform rather than floating. Accepted screenshots: `C:\Users\12156\AppData\Local\Temp\3dling-arch-hardfit-accept-1781749762482\desktop-1398x986-sence5.png`, `desktop-1398x986-feet-platform-crop.png`, `desktop-1440x900-feet-platform-crop.png`, `mobile-390x844-sence5.png`, and `mobile-390x844-feet-platform-crop.png`.
- User lighting issue on 2026-06-18 after hard `sence5` platform-fit acceptance: the toy now stands on the circular platform, but the cool overhead spotlight no longer visibly lands on the toy's hair/head/upper face. Treat the hard platform fit as the locked baseline; the next narrow fix should make the `sence5` spotlight/key-light visually follow the toy's head and shoulders without moving the boots off the platform or touching background/hash/other-scene behavior.
- User clarification on 2026-06-18 during `sence5` spotlight review: the toy position may change as long as it remains on the circular platform. The primary acceptance target is not just adding a local face/key-light overlay; the visible overhead light column and the toy should read as basically on the same vertical axis in the 1398px desktop preview. Return to development for a position/beam alignment pass, preserving the platform-grounding proof and existing background/hash/crossfade behavior.
- Architecture acceptance on 2026-06-18 after `sence5` light-column/toy-axis fix: passed. Code inspection confirmed the callback stayed narrow: `src\data\scenes.ts` only moves the `sence5` sprite horizontally from `[-0.3, 1.12, 0.08]` to `[-0.05, 1.12, 0.08]` and mobile from `[-0.28, 0.32, 0.08]` to `[-0.16, 0.32, 0.08]`; height, Y landing point, background image, hash routing, photo stack, crossfade, other scenes, and global `STAGE_ANCHORS` were not changed. Local `npm run build` passed with only the existing Three.js chunk-size warning. Browser plugin DOM state passed for direct `#sence5`, but its screenshot surface showed a stale placeholder-toy frame inconsistent with `data-model-state="sprite"`, so final visual acceptance used a fresh Playwright + local Chrome context. Verified direct `#sence5` at 1398x986, 1440x900, and 390x844 plus clicked navigation `#sence1 -> #sence5 -> #sence6`, with `canvasCount=1`, no console/page/request errors, no horizontal overflow, and preserved background crossfade. Visual acceptance: desktop 1398 and 1440 now read as the visible cool spotlight column landing through the toy's head/upper body while both boot soles remain on the circular platform top surface. Accepted screenshots: `C:\Users\12156\AppData\Local\Temp\3dling-arch-axis-accept-1781753893442\desktop-1398x986-sence5.png`, `desktop-1398x986-light-axis-head-crop.png`, `desktop-1398x986-feet-platform-crop.png`, `desktop-1440x900-sence5.png`, and `mobile-390x844-sence5.png`.
- User responsive-layout issue on 2026-06-18 after the `sence5` 1398/1440 axis acceptance: at a wider viewport, user-supplied screenshot `C:\Users\12156\AppData\Local\Temp\codex-clipboard-88536041-1dd5-4daf-a7f0-555c210bc3d2.png` shows the toy drifting right relative to the visible overhead light column and circular stage. Treat this as a responsive alignment bug: `sence5` toy placement must track the generated background's visible light/stage landmarks across viewport widths, not only at 1398/1440. Next development pass should validate at least 1398x986, 1440x900, 1920x1080, the user's wide viewport around 2048x1117, and 390x844 mobile.
- Architecture acceptance on 2026-06-18 after `sence5` responsive light/stage anchor fix: passed. Code inspection confirmed the callback stayed scoped: `src\data\scenes.ts` adds `toyDisplay.responsivePositions` for `sence5`, while `src\components\StageCanvas.tsx` centralizes aspect-ratio interpolation in `interpolateSpritePosition()` / `resolveSpritePosition()` / `applySpritePlacement()` and reapplies placement on resize; background image, hash routing, photo stack, crossfade, other scenes, GLB default, and global `STAGE_ANCHORS` were not changed. Local `npm run build` passed with only the existing Three.js chunk-size warning. Browser plugin DOM check passed for direct `#sence5`; final visual acceptance used fresh Playwright + local Chrome. Verified direct `#sence5` at 1398x986, 1440x900, 1920x1080, 2048x1117, and 390x844, plus same-page resize from 1398x986 to 2048x1117 and clicked navigation `#sence1 -> #sence5 -> #sence6`; all had `canvasCount=1`, no console/page/request errors, no horizontal overflow, and preserved background crossfade. Visual acceptance: 2048x1117 no longer shows the toy drifting to the right of the light/stage; the toy reads as under the visible cool spotlight and remains on the circular platform top surface. Accepted screenshots: `C:\Users\12156\AppData\Local\Temp\3dling-arch-responsive-accept-1781776982661\desktop-2048x1117-sence5.png`, `desktop-2048x1117-light-toy-platform-crop.png`, `desktop-2048x1117-feet-platform-crop.png`, `resize-1398-to-2048-sence5.png`, `desktop-1920x1080-sence5.png`, and `mobile-390x844-sence5.png`.
